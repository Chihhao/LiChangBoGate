#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// --- Wi-Fi 多組設定 ---
// 請在此處定義三組 SSID 與密碼，供輪流嘗試連線使用
const char* ssid_list[] = {
  "YOUR_WIFI_SSID_1",
  "YOUR_WIFI_SSID_2",
  "YOUR_WIFI_SSID_3"
};

const char* password_list[] = {
  "YOUR_WIFI_PASSWORD_1",
  "YOUR_WIFI_PASSWORD_2",
  "YOUR_WIFI_PASSWORD_3"
};

// --- MQTT 伺服器與主題 ---
const char* mqttServer = "mqttgo.io";
const int mqttPort = 1883;
const char* TOPIC = "YOUR_MQTT_TOPIC";

// --- OTA (Over-The-Air) 更新設定 ---
const char* ota_hostname = "esp32-gate-control";
const char* ota_password = "YOUR_OTA_PASSWORD";

#endif // CREDENTIALS_H
